
description: GPIO pin to switch status LED on or off

compatible: "status_led"

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      The GPIO connected to the status LED